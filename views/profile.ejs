<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign-Up</title>
    <link href="/stylesheets/output.css" rel="stylesheet">
</head>
<body class="w-full min-h-screen flex flex-col justify-center items-center font-sans bg-zinc-900 text-amber-50 p-4">
    <div class="absolute top-6 right-6">
        <a class="bg-amber-600 hover:bg-amber-500 text-zinc-900 font-semibold px-4 py-2 rounded-md transition-all duration-200" href="/login">Logout</a>
    </div>
    <div class="bg-zinc-800 p-8 rounded-2xl shadow-xl w-full max-w-md space-y-8 border border-zinc-700">
        <div class="flex gap-2">
            <div class="flex flex-col items-center gap-2">
                <div class="relative w-20 h-20 rounded-full overflow-hidden group shadow-md">
                    <img src="/images/uploads/<%= user.profilePic %>" class="w-full h-full object-cover" alt="">
                    <a href="/upload/profile" class="absolute inset-0 bg-black/60 flex items-center justify-center text-amber-400 font-bold opacity-0 group-hover:opacity-100 transition-opacity duration-300 text-sm">
                    Update
                    </a>
                </div>
           </div>     
            <h1 class="font-bold text-3xl text-center mb-6">Hello, <span class="text-amber-400"></span> <%= user.name%></h1>
        </div>
        <div class="space-y-1">
            <h3 class="text-lg font-semibold text-amber-400">Create a Post</h3>
            <form action="/post" method="post" class="flex flex-col space-y-4">
                <textarea name="postData" id="" placeholder="What's on your mind?"
                    class="bg-zinc-900 text-amber-50 placeholder-zinc-500 rounded-lg p-3 resize-none outline-none"
                    rows="4"
                    ></textarea>
                <button type="submit" class="bg-amber-600 hover:bg-amber-500 text-zinc-900 font-semibold py-2 rounded-md transition-all duration-200">Create post</button>
            </form>
        </div>
        <div class="space-y-4">
            <h5 class="text-lg font-semibold text-amber-400 border-b border-zinc-700 pb-1">Your Posts</h5>
            <div class="postContainer space-y-2">
                <% user.posts.reverse().forEach(function(post){ %>
                    <div class="bg-zinc-900 p-4 rounded-lg border border-zinc-700 shadow-inner">
                    <h4 class="font-semibold text-amber-300 ">@<%= user.username%></h4>
                    <p class="text-md text-zinc-200 leading-relaxed mt-2"><%= post.postData %></p>
                    <p class="text-sm text-zinc-300 leading-relaxed"><%= post.likes.length %> likes</p>
                    <div class="btn flex justify-end gap-3 pt-2">
                        <a href="/like/<%= post._id %>" class="bg-amber-600 hover:bg-amber-500 text-zinc-900 font-semibold px-3 py-1 rounded-md text-sm transition-all duration-200">
                            <%= post.likes.indexOf(user._id)==-1? "Like":"Unlike" %>
                        </a>
                        <a href="/edit/<%= post._id %>" class="bg-zinc-800 hover:bg-zinc-700 border border-zinc-600 text-amber-300 font-semibold px-3 py-1 rounded-md text-sm transition-all duration-200">Edit</a>
                    </div>
                </div>
              <%  }) %>
            </div>
        </div>
    </div>
</body>
</html>